package GUI.view;

import java.awt.event.ActionListener;

public interface WelcomePageInterface {

    void addLoginListener(ActionListener action);

    void addRegisterListener(ActionListener action);

}